# Regulation Radar — backend dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-dotenv>=1.0

# Agent orchestration. Always needed.
langgraph>=0.2

# Model providers — the app runs WITHOUT any of these (deterministic stub mode).
# Install the one matching the key you set: Gemini OR Anthropic. Both is fine too.
google-genai>=0.3
anthropic>=0.40
